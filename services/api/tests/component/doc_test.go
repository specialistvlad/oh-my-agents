// Package component_test holds tests that talk to real infrastructure.
// `make check` loads ../../.env before running them; `make check-ci` skips
// the package entirely, since CI has no infra to talk to.
package component_test
